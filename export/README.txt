Export data from database with the following command

bash export_data.sh <dbname> <output dir>

export_data.sh -  calls export_data.py and takes the db as well as a location as arguements
export_data.py - takes data in the db and converts it into csvs
